<% @products.each do  |product| %>
  <%= product.brand %>
  <%= product.model %>
  <%= product.description %>
  <%= product.price %>
  <%= link_to image_tag(product.product_photo.url), product_path(product) %>
<% end %>

